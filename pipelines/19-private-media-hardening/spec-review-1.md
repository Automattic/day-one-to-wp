# Spec review 1

Result: Approved.

The spec addresses the reopened issue by removing newly imported media from the public uploads tree instead of relying on server-specific protection files. It keeps the permission-checked endpoint and explicitly requires safe failure when private storage cannot be prepared.
