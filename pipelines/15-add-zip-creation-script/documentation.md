# Documentation summary

Documentation was updated in `README.md` with a new "Build a plugin ZIP" section.

The section tells developers to run:

```sh
npm install
npm run plugin-zip
```

It also notes that the ZIP is generated in the repository root and that included files are controlled by the `files` list in `package.json`.
